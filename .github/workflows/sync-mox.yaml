name: Sync MOx CSS

on:
  schedule:
    - cron: "0 6 * * 1" # Every Monday at 06:00 UTC
  workflow_dispatch: # Allow manual trigger

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Clone latest poc-moza
        run: git clone --depth 1 https://github.com/MinBZK/poc-moza.git /tmp/poc-moza

      - name: Sync CSS files
        run: |
          rm -rf static/style
          mkdir -p static/style
          cp /tmp/poc-moza/style/style.css static/style/
          cp /tmp/poc-moza/style/_mox.css static/style/
          cp /tmp/poc-moza/style/_rijkshuisstijl.css static/style/
          cp /tmp/poc-moza/style/_reset.css static/style/

      - name: Sync assets
        run: |
          rm -rf static/assets
          cp -r /tmp/poc-moza/assets static/assets

      - name: Sync design tokens
        run: |
          rm -rf tokens
          mkdir -p tokens
          cp /tmp/poc-moza/tokens/tokens.json tokens/

      - name: Sync storybook stories
        run: |
          rm -rf stories
          cp -r /tmp/poc-moza/stories stories

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet && [ -z "$(git ls-files --others --exclude-standard)" ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Create Pull Request
        if: steps.changes.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: "Update MOx CSS and assets to latest version"
          title: "Update MOx CSS and assets to latest version"
          body: |
            Automated sync of MOx design system CSS, assets, tokens, and stories from
            [MinBZK/poc-moza](https://github.com/MinBZK/poc-moza).
          branch: sync-mox-css
          delete-branch: true
